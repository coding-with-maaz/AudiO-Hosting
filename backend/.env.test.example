# Test Environment Configuration
NODE_ENV=test
PORT=3000

# Test Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=audio_hosting_db_test
DB_USER=root
DB_PASSWORD=

# JWT Configuration for Testing
JWT_SECRET=test-jwt-secret-key-for-testing-only-do-not-use-in-production
JWT_EXPIRE=1h

# File Upload Configuration (Test)
UPLOAD_DIR=./uploads/audio/test
MAX_FILE_SIZE=104857600
ALLOWED_AUDIO_TYPES=audio/mpeg,audio/mp3,audio/wav,audio/ogg,audio/aac,audio/flac

# CORS Configuration (Test)
CORS_ORIGIN=http://localhost:3001

# Email Configuration (Test - Mock or use test SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=test@audiohosting.com

# Redis Configuration (Test - Optional)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Base URL (Test)
BASE_URL=http://localhost:3000

